import Layout from "@/components/layout/Layout";
import { Box, Heading } from "@chakra-ui/react";

const PatientSettings = () => {
  return (
    <Layout>
      <Box p={10}>
        <Heading size="lg">Patient settings</Heading>
      </Box>
    </Layout>
  );
};

export default PatientSettings;
