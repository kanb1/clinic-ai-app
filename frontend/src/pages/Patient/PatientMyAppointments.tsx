import Layout from "@/components/layout/Layout";
import { Box, Heading } from "@chakra-ui/react";

const PatientMyAppointments = () => {
  return (
    <Layout>
      <Box p={10}>
        <Heading size="lg">Patient My Appointments</Heading>
      </Box>
    </Layout>
  );
};

export default PatientMyAppointments;
