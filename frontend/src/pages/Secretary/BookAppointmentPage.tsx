import Layout from "@/components/layout/Layout";
import { Box, Heading } from "@chakra-ui/react";

const BookingPage = () => {
  return (
    <Layout>
      <Box p={10}>
        <Heading size="lg">Sekretær Bookingpage</Heading>
      </Box>
    </Layout>
  );
};

export default BookingPage;
